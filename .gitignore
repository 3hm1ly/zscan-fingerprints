# intermediate files (workflows produce them in CWD then commit only artifacts/)
web_fingerprint_v4.json
*.tar.gz
__pycache__/
*.pyc

# DO commit artifacts/ — that's the JSDelivr-served payload
!artifacts/
!artifacts/**
